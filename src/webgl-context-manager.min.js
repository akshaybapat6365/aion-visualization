class WebGLContextManager{constructor(){this.activeContexts=new Map();this.contextPool=[];this.maxContexts=4;this.memoryThreshold=150*1024*1024;this.performanceMonitor=new PerformanceMonitor();this.isLowMemoryDevice=this.detectLowMemoryDevice();if(this.isLowMemoryDevice){this.maxContexts=3;this.memoryThreshold=75*1024*1024;}this.setupMemoryMonitoring();}detectLowMemoryDevice(){if(navigator.deviceMemory){return navigator.deviceMemory<=4;}const isMobile=/Android|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);return isMobile;}setupMemoryMonitoring(){setInterval(()=>{this.checkMemoryUsage();},10000);document.addEventListener('visibilitychange',()=>{if(document.hidden){this.pauseAllContexts();}else{this.resumeAllContexts();}});window.addEventListener('beforeunload',()=>{this.cleanupAllContexts();});}checkMemoryUsage(){if(performance.memory){const memoryUsage=performance.memory.usedJSHeapSize;if(memoryUsage>this.memoryThreshold){}MB`);this.performMemoryCleanup();}}}performMemoryCleanup(){if(window.gc&&typeof window.gc==='function'){window.gc();}const sortedContexts=Array.from(this.activeContexts.entries()).sort((a,b)=>a[1].lastUsed-b[1].lastUsed);const contextsToClean=Math.ceil(sortedContexts.length*0.3);for(let i=0;i<contextsToClean&&i<sortedContexts.length;i++){const[contextId]=sortedContexts[i];this.releaseContext(contextId);}}acquireContext(canvas,options={}){const contextId=canvas.id||`context-${Date.now()}-${Math.random()}`;if(this.activeContexts.has(contextId)){const contextData=this.activeContexts.get(contextId);contextData.lastUsed=Date.now();return contextData.gl;}if(!this.canCreateNewContext()){return this.getFallbackContext(canvas);}let gl=this.getPooledContext(canvas,options);if(!gl){gl=this.createNewContext(canvas,options);}if(gl){this.activeContexts.set(contextId,{gl:gl,canvas:canvas,created:Date.now(),lastUsed:Date.now(),memoryUsage:this.estimateContextMemory(canvas),options:options,isPaused:false});this.setupContextMonitoring(contextId,gl,canvas);}return gl;}canCreateNewContext(){if(this.activeContexts.size>=this.maxContexts){return false;}if(performance.memory){const memoryUsage=performance.memory.usedJSHeapSize;const projectedUsage=memoryUsage+(30*1024*1024);if(projectedUsage>this.memoryThreshold){return false;}}return true;}getPooledContext(canvas,options){for(let i=0;i<this.contextPool.length;i++){const pooledContext=this.contextPool[i];if(this.isContextCompatible(pooledContext.options,options)){this.contextPool.splice(i,1);return this.adaptContextToCanvas(pooledContext.gl,canvas);}}return null;}createNewContext(canvas,options){const contextAttributes={alpha:options.alpha!==false,antialias:this.isLowMemoryDevice?false:(options.antialias!==false),depth:options.depth!==false,preserveDrawingBuffer:options.preserveDrawingBuffer||false,powerPreference:this.isLowMemoryDevice?'low-power':(options.powerPreference||'default'),failIfMajorPerformanceCaveat:this.isLowMemoryDevice};let gl=null;try{if(!this.isLowMemoryDevice&&options.preferWebGL2!==false){gl=canvas.getContext('webgl2',contextAttributes);}if(!gl){gl=canvas.getContext('webgl',contextAttributes)||canvas.getContext('experimental-webgl',contextAttributes);}if(gl){this.setupContextLossHandling(gl,canvas);}}catch(error){console.error('WebGL context creation failed:',error);return null;}return gl;}setupContextLossHandling(gl,canvas){canvas.addEventListener('webglcontextlost',(event)=>{event.preventDefault();this.handleContextLoss(canvas,event);});canvas.addEventListener('webglcontextrestored',(event)=>{this.handleContextRestore(canvas,event);});}setupContextMonitoring(contextId,gl,canvas){const originalDrawArrays=gl.drawArrays;const originalDrawElements=gl.drawElements;let drawCallCount=0;gl.drawArrays=function(...args){drawCallCount++;return originalDrawArrays.apply(this,args);};gl.drawElements=function(...args){drawCallCount++;return originalDrawElements.apply(this,args);};setInterval(()=>{const contextData=this.activeContexts.get(contextId);if(contextData){contextData.drawCalls=drawCallCount;contextData.lastUsed=Date.now();drawCallCount=0;}},5000);}releaseContext(contextId){const contextData=this.activeContexts.get(contextId);if(!contextData)return;const{gl,canvas,options}=contextData;this.cleanupWebGLResources(gl);if(this.contextPool.length<3&&!this.isLowMemoryDevice){this.contextPool.push({gl:gl,options:options,pooled:Date.now()});}canvas.width=1;canvas.height=1;this.activeContexts.delete(contextId);}cleanupWebGLResources(gl){const extensions=gl.getSupportedExtensions();const numAttributes=gl.getParameter(gl.MAX_VERTEX_ATTRIBS);for(let i=0;i<numAttributes;i++){gl.disableVertexAttribArray(i);}gl.bindBuffer(gl.ARRAY_BUFFER,null);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,null);gl.bindTexture(gl.TEXTURE_2D,null);gl.bindTexture(gl.TEXTURE_CUBE_MAP,null);gl.bindFramebuffer(gl.FRAMEBUFFER,null);gl.bindRenderbuffer(gl.RENDERBUFFER,null);gl.useProgram(null);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);}pauseAllContexts(){this.activeContexts.forEach((contextData,contextId)=>{contextData.isPaused=true;});}resumeAllContexts(){this.activeContexts.forEach((contextData,contextId)=>{contextData.isPaused=false;contextData.lastUsed=Date.now();});}cleanupAllContexts(){const contextIds=Array.from(this.activeContexts.keys());contextIds.forEach(contextId=>{this.releaseContext(contextId);});this.contextPool=[];}estimateContextMemory(canvas){const pixelCount=canvas.width*canvas.height;const bytesPerPixel=4;const baseMemory=pixelCount*bytesPerPixel;return baseMemory*2;}isContextCompatible(options1,options2){const importantOptions=['alpha','antialias','depth','powerPreference'];return importantOptions.every(option=>options1[option]===options2[option]);}adaptContextToCanvas(gl,newCanvas){return gl;}getFallbackContext(canvas){return null;}handleContextLoss(canvas,event){for(const[contextId,contextData]of this.activeContexts.entries()){if(contextData.canvas===canvas){contextData.isLost=true;break;}}if(window.VisualizationErrorBoundary){const errorBoundary=new window.VisualizationErrorBoundary(canvas.parentElement);errorBoundary.showWebGLFallback();}}handleContextRestore(canvas,event){for(const[contextId,contextData]of this.activeContexts.entries()){if(contextData.canvas===canvas){contextData.isLost=false;break;}}}getStatistics(){const stats={activeContexts:this.activeContexts.size,pooledContexts:this.contextPool.length,memoryUsage:performance.memory?Math.round(performance.memory.usedJSHeapSize/1024/1024):'Unknown',memoryLimit:Math.round(this.memoryThreshold/1024/1024),isLowMemoryDevice:this.isLowMemoryDevice,contexts:[]};this.activeContexts.forEach((contextData,contextId)=>{stats.contexts.push({id:contextId,age:Date.now()-contextData.created,lastUsed:Date.now()-contextData.lastUsed,memoryEstimate:Math.round(contextData.memoryUsage/1024/1024),isPaused:contextData.isPaused,isLost:contextData.isLost||false});});return stats;}}class PerformanceMonitor{constructor(){this.metrics={frameRate:0,frameTime:0,memoryUsage:0,drawCalls:0};this.startTime=performance.now();this.frameCount=0;this.lastFrameTime=this.startTime;this.setupMonitoring();}setupMonitoring(){this.monitorFrameRate();if(performance.memory){setInterval(()=>{this.metrics.memoryUsage=performance.memory.usedJSHeapSize;},1000);}}monitorFrameRate(){const measure=()=>{const currentTime=performance.now();const deltaTime=currentTime-this.lastFrameTime;this.frameCount++;this.metrics.frameTime=deltaTime;if(currentTime-this.startTime>=1000){this.metrics.frameRate=Math.round((this.frameCount*1000)/(currentTime-this.startTime));this.frameCount=0;this.startTime=currentTime;}this.lastFrameTime=currentTime;requestAnimationFrame(measure);};requestAnimationFrame(measure);}getMetrics(){return{...this.metrics};}isPerformanceGood(){return this.metrics.frameRate>=30&&this.metrics.frameTime<33.33;}}class SmartAssetLoader{constructor(){this.loadStrategy=this.determineStrategy();this.preloadQueue=new PriorityQueue();this.memoryBudget=this.calculateMemoryBudget();this.networkSpeed=this.estimateNetworkSpeed();}determineStrategy(){const connection=navigator.connection||navigator.mozConnection||navigator.webkitConnection;const deviceMemory=navigator.deviceMemory||4;if(connection?.effectiveType==='4g'&&deviceMemory>=8){return 'aggressive';}else if(connection?.effectiveType==='3g'||deviceMemory<4){return 'conservative';}return 'balanced';}calculateMemoryBudget(){const deviceMemory=navigator.deviceMemory||4;return Math.min(deviceMemory*0.25*1024*1024*1024,500*1024*1024);}estimateNetworkSpeed(){const connection=navigator.connection||navigator.mozConnection||navigator.webkitConnection;if(connection){return connection.downlink||1;}return 1;}shouldLoadVisualization(){const memoryUsage=performance.memory?performance.memory.usedJSHeapSize:0;return memoryUsage<this.memoryBudget&&this.networkSpeed>0.5;}async loadChapterAssets(chapterId){if(!this.shouldLoadVisualization()){return;}}}class PriorityQueue{constructor(){this.items=[];}enqueue(item,priority){const queueElement={item,priority};let added=false;for(let i=0;i<this.items.length;i++){if(queueElement.priority>this.items[i].priority){this.items.splice(i,0,queueElement);added=true;break;}}if(!added){this.items.push(queueElement);}}dequeue(){return this.items.shift();}isEmpty(){return this.items.length===0;}}window.webglContextManager=new WebGLContextManager();window.createOptimizedWebGLContext=function(canvas,options={}){return window.webglContextManager.acquireContext(canvas,options);};if(typeof module!=='undefined'&&module.exports){module.exports={WebGLContextManager,PerformanceMonitor,SmartAssetLoader};}