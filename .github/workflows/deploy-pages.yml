name: Legacy Pages deploy

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: echo "This legacy raw-src deploy is disabled. Use deploy.yml, which builds Vite and deploys dist."
